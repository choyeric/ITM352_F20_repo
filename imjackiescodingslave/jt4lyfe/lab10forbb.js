var product_quantities = [2,1,1,3,12]; 

for (i = 1; i <= 5; i++) {
    if (i = 1) {
    var product_quantities = [product[i].quantity]
    }
    else {
    product_quantities = [item[i].quantity[i]]
    }
  }

document.write(product_quantities.length);

product_quantities.push(product[i].quantity);

product_quantities.pop();